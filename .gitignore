#
# Secrets — never commit
#
.env
.env.*

#
# NLTK corpora (downloaded for textstat / readability)
#
nltk_data/

#
# Python
#
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
.envrc
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
coverage.xml
*.egg-info/
.eggs/
dist/
build/

#
# OS / editors
#
.DS_Store
.vscode/
.idea/
*.swp
*.swo

#
# Local logs & debug payloads
#
tmp_*.json

#
# Generated pipeline outputs (regenerate via scripts)
# Anchored to repo root where applicable
#
/drug_labels.json
/drug_labels.csv

/data/drug_labels.json
/data/drug_labels.csv
/data/drug_labels_test.json

#
# Large regenerable pipeline outputs (re-run to recreate)
#
outputs/simplified_labels.json
outputs/extracted_original.json
outputs/extracted_simplified.json
outputs/pipeline.log

# Per-run large JSON (regenerate via run_full_evaluation.sh); keep reports + readability tracked
outputs/runs/*/simplified_labels.json
outputs/runs/*/extracted_original.json
outputs/runs/*/extracted_simplified.json

# Research / paper artifacts under outputs/ are tracked (not ignored):
#   comparison_report.json, evaluation_report.json, analysis_results.json,
#   readability.json, manual_validation.json, validation_results.json

data/processed_labels/

#
# Paper / LaTeX (submitted separately as PDF)
#
paper.tex
checklist.tex
neurips_2026.sty
*.aux
*.log
*.out
*.bbl
*.blg
*.fls
*.fdb_latexmk
*.synctex.gz
