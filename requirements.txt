# Runtime: Groq and OpenAI simplification use the OpenAI-compatible client.
# (Pinned to versions in .venv — refresh after upgrading the venv.)
openai==2.31.0

# Readability metrics (original vs simplified); pulls nltk for syllable counts.
textstat==0.7.13

# Tests
pytest==9.0.3
